<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:layout_gravity="bottom"
    android:orientation="vertical"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <ImageView
        android:id="@+id/nav_image"
        android:layout_height="200dp"
        android:layout_width="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:src="@drawable/galaxy"/>
    <View
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:background="@color/purple_500"
        app:layout_constraintBottom_toTopOf="@id/title_nav_header"
        app:layout_constraintTop_toBottomOf="@+id/nav_image"
        />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/title_nav_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:fontFamily="@font/alfa_slab_one"
        android:text="@string/title_nav_header"
        android:textAlignment="center"
        android:textSize="@dimen/title_nav_header_text_size"
        app:layout_constraintTop_toBottomOf="@+id/nav_image"
        app:layout_constraintStart_toStartOf="parent"/>



</androidx.constraintlayout.widget.ConstraintLayout>